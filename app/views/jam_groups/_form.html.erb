<%= form_for(jam_group) do |f| %>
  <% if jam_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(jam_group.errors.count, "error") %> prohibited this jam_group from being saved:</h2>

      <ul>
      <% jam_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :image_url %>
    <%= f.text_field :image_url, class: "form-control"  %>
  </div>

  <div class="form-group">
    <%= f.label :description %>
    <%= f.text_area :description, class: "form-control"  %>
  </div>

  <div class="actions">
    <%= f.submit "Save Jam Group", class: "btn btn-primary" %>
  </div>
<% end %>
